type x = {int, a = text, b = [bool]}

type y = [int]

type z = bool

type u = enum {
  f = bool,
  g,
  h = {a = int, b = float}
}

type v = enum { Yes, No, Maybe }

type i = {v, z}

type Tree = enum {
    Leaf = int,
    Node = {left = Tree, right = Tree},
}

type Rel = {
  n_rows = int,
  page = [{
    id = int,
    name = text,
    address = {
      city = text,
      street = text
    },
    int,
    is_admin = bool
  }]
}
