type x: {Int64, a: Text, b: [Bool]}

type y: [Int64]

type z: Bool

type u: enum {
  f: Bool,
  g,
  h: {a: Int64, b: Float64}
}

type v: enum { Yes, No, Maybe }

type t: enum { single: Int8, double: Int16 }

type p: {[Int64], [Int64]}

type i: {v, z}

type Tree: enum {
  Leaf: Int8,
  Node: {left: Tree, right: Tree},
}

type opt: enum { None, Some: Text }

type opt2: enum { None, Some: Int16 }
