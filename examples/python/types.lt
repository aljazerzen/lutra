type Movie: {
  id: int64,
  title: text,
  is_released: bool,
  genre: Genre,
}

type Genre: enum {
  fiction,
  drama,
  other: text
}

func get_movie(): Movie -> {
  id = 54, title = "Hello", is_released = true, genre = .drama
}
