
module db {
  type invoice_ty = {id = int64, transaction_fee = float64, total = float64}

  let invoices: func (): [invoice_ty]
}

let my_func2 = func (x: int64): int64 -> x + 1

let my_func = func (x: int64): {int64, int64} -> {x, my_func2(x)}

{a = 1 + 2, my_func(3), my_func(2)}
